apiVersion: v1
data:
  ACME_EMAIL: your_email@example.com

  BASE_API_SLUG: api
  CLIENT_URL: http://client
  DB_HOST: db
  DB_NAME: chatapp
  DB_PORT: "5432"
  DB_USER: username
  DOMAIN: localhost
  GRAFANA_PASSWORD: your_grafana_password_here

  LOG_LEVEL: info
  NODE_ENV: development
  SERVER_PORT: "3000"
  REDIS_HOST: redis
  REDIS_PORT: "6379"
  REDIS_URL: redis://redis:6379

  TRAEFIK_LOG_LEVEL: INFO
  VITE_API_URL: /api
  VITE_APP_NAME: Chat App
  VITE_APP_VERSION: 1.0.0
  VITE_DEV_MODE: "true"
  # Allow localhost and NodePort for Minikube access (adjust as needed)
  CORS_ORIGINS: http://localhost:5173,http://localhost:30080
  # Nginx proxy configuration
  SERVER_HOST: server.default.svc.cluster.local
  SERVER_PORT: "3000"
kind: ConfigMap
metadata:
  labels:
    io.kompose.service: client-env
  name: env
