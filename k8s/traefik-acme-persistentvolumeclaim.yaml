apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: traefik-acme
  name: traefik-acme
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
