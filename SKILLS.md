# Project Skills

The canonical repository skill is [`chat-app-engineering`](.agents/skills/chat-app-engineering/SKILL.md).

Compatible coding agents discover it from `.agents/skills/`. Invoke `$chat-app-engineering` explicitly or let the agent load it for planning, implementation, debugging, review, testing, security, AI behavior, data, dependency, deployment, and release work in this repository.

This compatibility file preserves the previous `SKILLS.md` entrypoint. Keep project workflow instructions in the canonical skill and its `references/` files so there is one source of truth and task-specific context loads only when needed.
